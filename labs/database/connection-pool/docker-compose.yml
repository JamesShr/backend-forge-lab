services:
  postgres:
    image: postgres:17
    environment:
      POSTGRES_DB: backend_forge_lab
      POSTGRES_USER: lab
      POSTGRES_PASSWORD: lab
    ports:
      - "55437:5432"
    tmpfs:
      - /var/lib/postgresql/data
